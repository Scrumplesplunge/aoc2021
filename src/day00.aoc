import "std/std.aoc" as std;

function main(): int64 {
  alias n = 1024;
  var buffer: [n]byte;
  var length: int64 = 1;
  while length {
    length = std.read(std.stdin, &buffer as *void, n);
    std.write(std.stdout, &buffer as *void, length);
  }
  return 0;
}
